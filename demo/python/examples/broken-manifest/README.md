# Broken Manifest Demo Example

## Purpose

This example is intended to demonstrate a manifest parsing failure in the Python CXF demo verifier.

Unlike the positive examples and cryptographic mismatch cases, this scenario is designed to fail before normal verification can begin.
