# Wrong Final Root Demo Example

## Purpose

This example demonstrates a container-level verification failure in which all chunk hashes verify successfully but the declared expected_final_root is intentionally incorrect.

This case distinguishes chunk-level correctness from final-root correctness.
